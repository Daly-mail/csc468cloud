apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-deployment
  labels:
    role: app
spec:
  replicas: 2
  selector:
    matchLabels:
      role: app
  template:
    metadata:
      labels:
        role: app
    spec:
      containers:
      - name: app
        image: ld953642/csc468cloud:mysql
        resources:
          requests:
            cpu: 10m
